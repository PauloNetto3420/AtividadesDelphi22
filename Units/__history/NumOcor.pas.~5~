unit NumOcor;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  TForm1 = class(TForm)
    Label1: TLabel;
    Edit1: TEdit;
    Button1: TButton;
    Edit2: TEdit;
    Label2: TLabel;
    Label3: TLabel;
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R *.dfm}

function NumOcor1(Cad : string; C : Char) : Integer;
var
  I : Integer;
begin

  for I := 1 to Length(Cad) do
    begin
      if Cad[i] = C then
      begin
        Result := Result + 1
      end
      else
      begin
        end;
    end;
    ShowMessage(IntToStr(Result));
  end;



end.
